---
import BaseLayout from "../../layouts/BaseLayout.astro"
import Card from "../../components/Card.astro"
const allPosts = Object.values(import.meta.glob('./posts/*.md',{eager:true}))

const uniqueTags = [...new Set(allPosts.map((post:any) => post.frontmatter.tags).flat())]
---

<BaseLayout>
    <!-- <section class="text-sm flex gap-2 mb-5 justify-center">
        {
            uniqueTags.map((tag:any) => <a href=`tags/${tag}` class="bg-amber-300 text-sm rounded-xl px-3">{tag}</a>)
        }
    </section> -->
    <section class="grid grid-cols-3 gap-8 mb-15">
        {
            allPosts.map((post:any) => <Card url={post.url} data={post.frontmatter} />)
        }
    </section>

</BaseLayout>